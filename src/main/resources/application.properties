spring.application.name=springboot-cicd-demo
server.port=8080

# Actuator endpoints
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.info.env.enabled=true

# App info (shows in /actuator/info)
info.app.name=Spring Boot CI/CD Demo
info.app.version=1.0.0
info.app.description=Demo app with Jenkins CI/CD pipeline

# Logging
logging.level.com.example.app=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
